parameters:
  paths:
    - src/Akeneo/Platform/ImportExport
    - src/Akeneo/Platform/Bundle/ImportExportBundle
  exclude_files:
    - '#^vendor/#'
    - '#^tests/#'

layers:
  - name: Shared
    collectors:
      - type: directory
        regex: '^src/Akeneo/(Tool|Platform)/'

  - name: Domain
    collectors:
      - type: directory
        regex: '^src/Akeneo/Platform/ImportExport/Component/'

  - name: Bundle
    collectors:
      - type: directory
        regex: '^src/Akeneo/Platform/Bundle/ImportExportBundle/'

ruleset:
  Bundle:
    - Domain
    - Shared
  Domain:
    - Shared
  Shared: []

skip_violations: []
