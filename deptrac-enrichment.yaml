parameters:
  paths:
    - src/Akeneo/Pim/Enrichment
  exclude_files:
    - '#^vendor/#'
    - '#^tests/#'

layers:
  - name: Shared
    collectors:
      - type: directory
        regex: '^src/Akeneo/(Tool|Platform)/'

  - name: Domain
    collectors:
      - type: directory
        regex: '^src/Akeneo/Pim/Enrichment/Component/'

  - name: Bundle
    collectors:
      - type: directory
        regex: '^src/Akeneo/Pim/Enrichment/Bundle/'

ruleset:
  Bundle:
    - Domain
    - Shared
  Domain:
    - Shared
  Shared: []

skip_violations: []
