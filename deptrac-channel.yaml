parameters:
  paths:
    - src/Akeneo/Channel
  exclude_files:
    - '#^vendor/#'
    - '#^tests/#'

layers:
  - name: Shared
    collectors:
      - type: directory
        regex: '^src/Akeneo/(Tool|Platform)/'

  - name: Domain
    collectors:
      - type: directory
        regex: '^src/Akeneo/Channel/Component/'

  - name: Bundle
    collectors:
      - type: directory
        regex: '^src/Akeneo/Channel/Bundle/'

ruleset:
  Bundle:
    - Domain
    - Shared
  Domain:
    - Shared
  Shared: []

skip_violations: []
