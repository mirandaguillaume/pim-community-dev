parameters:
  paths:
    - src/Akeneo/Pim/Structure
  exclude_files:
    - '#^vendor/#'
    - '#^tests/#'

layers:
  - name: Shared
    collectors:
      - type: directory
        regex: '^src/Akeneo/(Tool|Platform)/'

  - name: Domain
    collectors:
      - type: directory
        regex: '^src/Akeneo/Pim/Structure/Component/'

  - name: Bundle
    collectors:
      - type: directory
        regex: '^src/Akeneo/Pim/Structure/Bundle/'

ruleset:
  Bundle:
    - Domain
    - Shared
  Domain:
    - Shared
  Shared: []

skip_violations: []
