includes:
    - baseline.neon

parameters:
  level: max
  paths:
    - %rootDir%/../../../src/Akeneo/Platform/Job
  excludePaths:
    - %rootDir%/../../../src/Akeneo/Platform/Job/back/tests/Specification
  reportUnmatchedIgnoredErrors: false
  ignoreErrors:
    # PHPStan 2.x — @immutable/readonly property assigned in tests
    - '#@readonly property .+ is assigned outside of its declaring class#'
