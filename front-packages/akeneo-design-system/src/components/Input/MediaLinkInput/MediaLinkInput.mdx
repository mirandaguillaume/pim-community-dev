import {useState} from 'react';
import {Meta, Story, Controls, Canvas} from '@storybook/blocks';
import {MediaLinkInput} from './MediaLinkInput';
import {FullscreenPreview} from '../../../storybook';
import {IconButton, Button} from '../../../components';
import {RefreshIcon, CopyIcon, DownloadIcon, FullscreenIcon} from '../../../icons';
import {useBooleanState} from '../../../hooks';
import * as MediaLinkInputStories from './MediaLinkInput.stories';

<Meta of={MediaLinkInputStories} />

# Media Link Input

## Usage

Media link input allows the user to enter content when the expected user input is a link.

## Playground

<Canvas>
  <Story of={MediaLinkInputStories.Standard} />
</Canvas>

<Controls />

# Variation on readOnly

<Canvas>
  <Story of={MediaLinkInputStories.ReadOnly} />
</Canvas>

# Variation on invalid

<Canvas>
  <Story of={MediaLinkInputStories.Invalid} />
</Canvas>
