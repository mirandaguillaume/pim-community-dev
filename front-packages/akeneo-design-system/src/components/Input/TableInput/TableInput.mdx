import {Meta, Canvas, Story, ArgTypes} from '@storybook/blocks';
import * as Stories from './TableInput.stories';
import {TableInput} from './TableInput.tsx';
import {Dropdown} from '../../Dropdown/Dropdown.tsx';
import {useFakeTableInput} from '../../../storybook/hooks/useFakeTableInput';
import {Image} from '../../Image/Image';
import {Badge} from '../../Badge/Badge';
import {IconButton} from '../../IconButton/IconButton';
import {LinkIcon} from '../../../icons';

<Meta of={Stories} />

# TableInput

## Usage

Table input allows the user to input content in a table.

## Playground

<Canvas of={Stories.Standard} />

<ArgTypes of={Stories.Standard} />

## Highlight cells

<Canvas of={Stories.HighlightCells} />

## Readonly

<Canvas of={Stories.Readonly} />

## Draggable

<Canvas of={Stories.Draggable} />

## Highlighted rows

<Canvas of={Stories.HighlightedRows} />
