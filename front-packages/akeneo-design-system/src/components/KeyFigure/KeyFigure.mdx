import {Meta, Canvas, Story, ArgTypes} from '@storybook/blocks';
import * as Stories from './KeyFigure.stories';
import {KeyFigure, KeyFigureGrid} from './KeyFigure';
import * as Icons from '../../icons';
import {Tooltip} from '../Tooltip/Tooltip';

<Meta of={Stories} />

# Key Figure

Key figures are used in dashboards to illustrate metrics.

## Playground

<Canvas of={Stories.Standard} />

<ArgTypes of={Stories.Standard} />

## With only a simple figure

<Canvas of={Stories.ValueOnly} />

## KeyFigure grid

<Canvas of={Stories.KeyFigureGrid} />
