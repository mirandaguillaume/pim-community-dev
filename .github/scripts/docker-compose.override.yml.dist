version: '3.7'

services:
  mysql:
    command: '--default-authentication-plugin=mysql_native_password --log_bin_trust_function_creators=1 --max-connections=500'
    tmpfs:
      - '/tmp'
      - '/var/lib/mysql'
