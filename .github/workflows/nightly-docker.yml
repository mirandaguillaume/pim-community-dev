name: Nightly Docker

on:
  schedule:
    - cron: "0 1 * * *"
  workflow_dispatch:

concurrency:
  group: nightly-docker-${{ github.workflow }}
  cancel-in-progress: false

env:
  CACHE_VERSION: v1
  PUBLISH_IMAGE: ${{ vars.PUBLISH_IMAGE || 'false' }}
  PIM_PHP_IMAGE: pim-php-dev:8.1

jobs:
  build:
    name: Build PHP image
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4

      - name: Build PHP image
        run: docker build -t ${{ env.PIM_PHP_IMAGE }} .

      - name: Save PHP image
        run: docker save ${{ env.PIM_PHP_IMAGE }} -o php-pim-image.tar

      - name: Upload image artifact
        uses: actions/upload-artifact@v4
        with:
          name: nightly-php-image
          retention-days: 7
          path: php-pim-image.tar

      - name: Log in to GHCR
        if: env.PUBLISH_IMAGE == 'true'
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Push image to GHCR
        if: env.PUBLISH_IMAGE == 'true'
        run: |
          tag=ghcr.io/${{ github.repository }}/pim-php-dev:nightly
          docker tag ${{ env.PIM_PHP_IMAGE }} "$tag"
          docker push "$tag"
